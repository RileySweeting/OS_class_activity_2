#include <stdio.h>

int main() {
    // Variables
    int count = 10;

    // Count down from 10 to 0
    while (count > 0) {
        printf("%d \n", count);
        count = count - 1;
    }

    // Display
    printf("Happy New Year! \n");

    // Return
    return 0;
}
